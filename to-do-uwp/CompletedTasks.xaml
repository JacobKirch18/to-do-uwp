<Page
    x:Class="to_do_uwp.CompletedTasks"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:to_do_uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:to_do_uwp.Models"
    xmlns:vm="using:to_do_uwp.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <CommandBar HorizontalAlignment="Right">
            <AppBarButton Icon="Home" Label="Home" Click="HomeButton_Click" Width="85" />
            <AppBarButton Icon="Setting" Label="Settings" Click="SettingsButton_Click" Width="85" />
            <AppBarButton Icon="Help" Label="About" Click="AboutButton_Click" Width="85" />
        </CommandBar>
        <TextBlock Text="Completed Tasks" FontSize="40" FontWeight="Bold" Margin="20" HorizontalAlignment="Center"/>
        <Button Content="Delete All" HorizontalAlignment="Center" Margin="20" Width="175" FontSize="18" Click="DeleteAllButton_Click"/>
        <ListView x:Name="ItemListView" Height="750" ItemsSource="{x:Bind CompletedItemsList.Items, Mode=OneWay}" Margin="20" HorizontalAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:ToDoItemViewModel">
                    <Grid Width="600" HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border BorderBrush="Gray" BorderThickness="1" Grid.Column="0">
                            <TextBlock Text="{x:Bind Name, Mode=OneWay}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <Border BorderBrush="Gray" BorderThickness="1" Grid.Column="1">
                            <TextBlock Text="{x:Bind DueDate, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <Button x:Name="DeleteButton" Content="Delete" Grid.Column="2" Click="DeleteButton_Click" HorizontalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
